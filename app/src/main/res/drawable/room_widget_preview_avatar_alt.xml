<?xml version="1.0" encoding="utf-8"?>
<!-- Second placeholder avatar colour for the widget picker preview, so the two sample
     senders are visually distinct (mirrors AvatarUtils' per-user colour palette). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="#9B4A8B" />
</shape>
