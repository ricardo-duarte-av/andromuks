<?xml version="1.0" encoding="utf-8"?>
<!--
  Static preview shown in the launcher's widget picker (android:previewLayout, API 31+).

  This is NOT the widget: it is a static mock of what the widget looks like once configured. The
  picker renders it without ever starting a Glance session, which is why pointing previewLayout at
  Glance's loading layout (as this did originally) produced a spinner that never resolved.

  IMPORTANT — the launcher inflates this as **RemoteViews**, so only the RemoteViews-supported
  widget classes may appear here: FrameLayout, LinearLayout, RelativeLayout, GridLayout, TextView,
  ImageView, ImageButton, Button, ProgressBar, Chronometer, AnalogClock and the adapter views.
  A plain <View> is NOT among them — using one for the avatar placeholders made inflation throw,
  which the launcher surfaces as a black preview and "couldn't add widget".

  Colours are explicit day/night resources rather than ?android:attr/... for the same reason: the
  theme resolved during inflation belongs to the launcher, not to us.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@drawable/room_widget_preview_card">

    <!-- Header: room avatar + room name + refresh affordance -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/room_widget_preview_avatar"
            android:contentDescription="@null" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="@color/room_widget_preview_text"
            android:text="@string/room_widget_preview_room" />

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/room_widget_preview_refresh"
            android:contentDescription="@null" />
    </LinearLayout>

    <!-- Sample message row 1 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:orientation="horizontal">

        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/room_widget_preview_avatar"
            android:contentDescription="@null" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxLines="1"
                android:textSize="12sp"
                android:textColor="@color/room_widget_preview_sender"
                android:text="@string/room_widget_preview_sender_one" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:maxLines="2"
                android:ellipsize="end"
                android:textSize="14sp"
                android:textColor="@color/room_widget_preview_text"
                android:text="@string/room_widget_preview_message_one" />
        </LinearLayout>
    </LinearLayout>

    <!-- Sample message row 2 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:orientation="horizontal">

        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/room_widget_preview_avatar_alt"
            android:contentDescription="@null" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxLines="1"
                android:textSize="12sp"
                android:textColor="@color/room_widget_preview_sender"
                android:text="@string/room_widget_preview_sender_two" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:maxLines="2"
                android:ellipsize="end"
                android:textSize="14sp"
                android:textColor="@color/room_widget_preview_text"
                android:text="@string/room_widget_preview_message_two" />
        </LinearLayout>
    </LinearLayout>
</LinearLayout>
